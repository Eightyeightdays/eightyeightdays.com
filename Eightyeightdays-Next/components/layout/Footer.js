import styles from "../../styles/layout/Footer.module.css"

export default function Footer(){
    return(
        <footer className={styles.footer}>
            <div>Some footer links & info</div>
        </footer>
    )
}